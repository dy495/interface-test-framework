<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haisheng.framework.dao.ICaseDao">
    <resultMap id="CaseMap" type="com.haisheng.framework.model.bean.Case">
        <result property="id" column="id"/>
        <result property="applicationId" column="application_id" />
        <result property="configId" column="config_id" />
        <result property="caseName" column="case_name" />
        <result property="createTime" column="create_time" />
        <result property="editTime" column="edit_time" />
        <result property="result" column="result" />
        <result property="expect" column="expect" />
        <result property="response" column="response" />
        <result property="requestData" column="request_data" />
        <result property="qaOwner" column="qa_owner" />
    </resultMap>

    <!-- 所有字段 -->
    <sql id="allFields">
        id,application_id,config_id,case_name,create_time,edit_time,result,expect,response,request_data,qa_owner
    </sql>


    <!-- insert application-->
    <insert id="insert"
            parameterType="com.haisheng.framework.model.bean.Case"
            useGeneratedKeys="true"
            keyProperty="id">
        <![CDATA[
            insert into t_case (
                id,
                application_id,
                config_id,
                case_name,
                expect,
                request_data,
                response,
                result,
                qa_owner,
                edit_time
            ) values (
                #{id},
                #{applicationId},
                #{configId},
                #{caseName},
                #{expect},
                #{requestData},
                #{response},
                #{result},
                #{qaOwner},
                #{editTime}
            ) ON DUPLICATE KEY UPDATE
            request_data=#{requestData},
            response=#{response},
            result=#{result},
            edit_time=#{editTime}
        ]]>
    </insert>

    <select id="queryCaseByName"
            resultType="java.lang.Integer">
        select
        id
        from
        t_case
        <where>
            application_id = #{applicationId}
            and config_id = #{configId}
            and case_name = #{caseName}
        </where>
    </select>
</mapper>